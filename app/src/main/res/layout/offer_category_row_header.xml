<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_expandable_header_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:id="@+id/textView_expandable_header_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:layout_marginLeft="15sp"
        android:layout_marginBottom="2dp"
        android:textStyle="bold"
        android:textSize="30sp"
        tools:text="Group 1"
        android:layout_marginStart="15sp" />
    <ImageView
        android:id="@+id/item_expandable_header_icon"
        android:layout_width="50sp"
        android:layout_height="50sp"
        android:layout_marginLeft="20sp"
        app:srcCompat="@drawable/ic_keyboard_arrow_down_black_24dp"
        android:layout_toRightOf="@id/textView_expandable_header_title"
        android:layout_toEndOf="@id/textView_expandable_header_title" />
</RelativeLayout>